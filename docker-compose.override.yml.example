# Docker Compose Override Example
# 
# This file provides an example of how to override specific settings
# for local development or troubleshooting.
#
# To use this file:
# 1. Copy it: cp docker-compose.override.yml.example docker-compose.override.yml
# 2. Uncomment the sections you need
# 3. Adjust values as needed
# 4. Run: docker compose up
#
# Docker Compose automatically applies docker-compose.override.yml if it exists.

services:
  # Uncomment this section if you're having volume mount issues on macOS
  # or want to use an absolute path
  # liquibase-init:
  #   volumes:
  #     - ${PWD}/observability/liquibase:/liquibase/changelog:ro

  # Uncomment to add memory limits for resource-constrained environments
  # gateway-service:
  #   mem_limit: 512m
  #   memswap_limit: 512m

  # order-service:
  #   mem_limit: 512m
  #   memswap_limit: 512m

  # inventory-service:
  #   mem_limit: 512m
  #   memswap_limit: 512m

  # Uncomment to reduce resource usage by using smaller observability images
  # or reducing replica counts
  # prometheus:
  #   mem_limit: 1g
  #   memswap_limit: 1g

  # loki:
  #   mem_limit: 512m
  #   memswap_limit: 512m

  # tempo:
  #   mem_limit: 512m
  #   memswap_limit: 512m

  # grafana:
  #   mem_limit: 512m
  #   memswap_limit: 512m
